section.main-page__list.preview
  h2.visually-hidden Список превью статей
  ul.preview__list
    each article in articles
      li.preview__item(class = article.picture ? `` : `preview__item--noimage`)
        ul.preview__breadcrumbs(style=`flex-wrap: wrap;`)
          each category in article.categories
            li.preview__breadcrumbs-item
              a.preview__breadcrumbs-link(href='#') #{category.name}
        +insertArticleImg(article)
        - const date = new Date(article.createdAt);
        time.preview__time(datetime=`${date.toISOString()}`)
          +formatDate(date)
        h3.preview__name
          a.preview__name-link(href=`/articles/${article.id}`) #{article.title}
        p.preview__text #{article.announce}
        a.preview__comment(href='#')
          | Комментарии
          span.preview__cloud
          |
          b.preview__comment-count #{article.comments.length}


